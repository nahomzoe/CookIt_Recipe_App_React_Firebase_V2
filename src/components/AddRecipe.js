import FormModal from "./FormModal";

const AddRecipe = () => {
  return (
    <div
      style={{
        backgroundColor: "black",
        textAlign: "center",
        paddingTop: "10rem",
        paddingBottom: "10rem",
      }}
    >
      <FormModal />
    </div>
  );
};

export default AddRecipe;
